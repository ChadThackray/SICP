#lang sicp

; I think the main problem with this approach is as you start
; to get large numbers, calculating b^n becomes impossible and 
; eventually you won't be able to store that in any reasonable
; sized integer format.